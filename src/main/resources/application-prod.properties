spring.application.name=ev-co-ownership-be
# Server port (Render sets PORT env var)
server.port=${PORT:8080}
# Database configuration (from external PostgreSQL - Supabase)
# Note: Supabase requires SSL connection. Ensure SPRING_DATASOURCE_URL includes ?sslmode=require
# Example: jdbc:postgresql://host:5432/dbname?sslmode=require
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
# Connection pool configuration for production
# Supabase transaction pool has limited connections, keep pool small
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1800000
# Connection leak detection - warn if connection not returned within 30 seconds
spring.datasource.hikari.leak-detection-threshold=30000
# Query timeout (30 seconds) - prevent long-running queries
spring.jpa.properties.jakarta.persistence.query.timeout=30000
spring.datasource.hikari.data-source-properties.loginTimeout=30
# Connection validation and retry
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.validation-timeout=10000
# Increase initialization timeout to allow database connection to establish (30 seconds)
spring.datasource.hikari.initialization-fail-timeout=30000
# PostgreSQL SSL configuration for Supabase
spring.datasource.hikari.data-source-properties.sslmode=require
spring.datasource.hikari.data-source-properties.ssl=true
# Increase connection timeouts for Supabase (network latency)
spring.datasource.hikari.data-source-properties.connectTimeout=120
spring.datasource.hikari.data-source-properties.socketTimeout=120
# Disable prepared statements for Supabase Connection Pooler (pgBouncer compatibility)
# pgBouncer doesn't support prepared statements well, so we disable them
spring.datasource.hikari.data-source-properties.prepareThreshold=0
# JPA/Hibernate config
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
# Disable open-in-view to prevent holding connections during HTTP request processing
spring.jpa.open-in-view=false
logging.level.org.springframework.boot.env=DEBUG
logging.level.com.group8.evcoownership.config=DEBUG
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.hibernate.engine.jdbc.env.internal=DEBUG
# Hibernate will auto-detect PostgreSQL dialect from JDBC connection
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Ho_Chi_Minh
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=quachda1133@gmail.com
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
payment.vnPay.url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
payment.vnPay.tmnCode=W97JHS5P
payment.vnPay.secretKey=${VN_PAY_SECRET_KEY}
payment.vnPay.returnUrl=${RENDER_PUBLIC_DOMAIN:https://ev-co-ownership.onrender.com}/api/funds/payments/callback
payment.vnPay.depositReturnUrl=${RENDER_PUBLIC_DOMAIN:https://ev-co-ownership.onrender.com}/api/deposits/deposit-callback
payment.vnPay.version=2.1.0
payment.vnPay.command=pay
payment.vnPay.orderType=other
contract.deposit.deadline.minutes=30
# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=3600000
#jwt.refresh-expiration=604800000
#jwt.remember-me-expiration=2592000000
# File Upload
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
# Request body size limit
server.tomcat.max-http-form-post-size=50MB
# Additional size limits
server.tomcat.max-connections=8192
# Frontend URL for production
app.frontend.url=https://ev-co-ownership-cost-sharing-system.vercel.app
frontend.base.url=http://localhost:3000
booking.checkin.earliest-offset-minutes=-1